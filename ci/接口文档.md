### 1. User login

**接口地址**: `/account/login`

**请求方式**: `POST`

**请求参数**:

| 参数名   | 类型   | 必填 | 说明     |
| -------- | ------ | ---- | -------- |
| account  | String | 是   | 用户账号 |
| password | String | 是   | 用户密码 |

**请求示例**:

```json
{
  "account": "user1",
  "password": "password123"
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
    "token": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "account": "user1",
    "name": "张三",
    "role": "user",
    "needChangePwd": false
  }
}
```

**响应字段说明**:

| 字段          | 类型          | 说明                                               |
| ------------- | ------------- | -------------------------------------------------- |
| token         | String        | 访问令牌，有效期 72 小时，后续请求需在请求头中携带 |
| account       | String        | 用户账号                                           |
| name          | String        | 用户姓名                                           |
| role          | String        | 用户角色 (admin 超级管理员 / user 普通用户)，取优先级最高者 |
| needChangePwd | Boolean       | 是否需要首次修改密码（本地用户首登改密为 true）     |

**业务说明**:

- 系统禁止以内置账号 `admin` 登录；真实管理员账号为 `admin_local`。
- `role` 为当前账号优先级最高的角色 code（多角色时取并集中的最高优先级角色）。

**错误码**:

| 状态码 | 说明                                                  |
| ------ | ----------------------------------------------------- |
| 400    | 参数错误：账号和密码不能为空 / 不能使用管理员账号登录 |
| 401    | 认证失败：账号或密码错误                              |

---

### 2. 刷新token（心跳保活）

**接口地址**: `/account/refresh-token`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌 |

**请求参数**: 无

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
    "token": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "account": "user1",
    "name": "张三",
    "role": "user"
  }
}
```

**响应字段说明**:

| 字段    | 类型    | 说明                                               |
| ------- | ------- | -------------------------------------------------- |
| token   | String  | 当前使用的访问令牌（与原token相同）                |
| account | String  | 用户账号                                           |
| name    | String  | 用户姓名                                           |
| role    | String  | 用户角色 (admin 超级管理员 / user 普通用户)，取优先级最高者 |

**业务说明**:

- 系统会从Token中获取当前登录用户信息
- TokenInterceptor会自动将token的过期时间刷新为72小时
- 前端可以定期调用此接口来保持用户登录状态，避免token过期
- 建议每隔30-60分钟调用一次此接口进行心跳保活

**错误码**:

| 状态码 | 说明                     |
| ------ | ------------------------ |
| 401    | 未授权：token无效或已过期 |
| 500    | token刷新失败：系统异常   |

---

### 3. 用户登出

**接口地址**: `/account/logout`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌 |

**请求参数**: 无

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": "登出成功"
}
```

**业务说明**:

- 系统会从Token中获取当前登录用户信息
- 从 Redis 中删除该 token，使 token 立即失效
- 登出后，该 token 无法再用于其他请求
- 前端应在用户点击登出按钮时调用此接口，并清除本地存储的 token

**错误码**:

| 状态码 | 说明                     |
| ------ | ------------------------ |
| 500    | 登出失败：系统异常        |

---

### 4. 获取文档所属人

**接口地址**: `/doc/owner`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌 |

**请求参数**:

| 参数名   | 类型   | 必填 | 说明         |
| -------- | ------ | ---- | ------------ |
| docId    | String | 是   | 文档唯一标识 |
| fileName | String | 否   | 文件名       |

**请求示例**:

```json
{
  "docId": "doc123456",
  "fileName": "test.docx"
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
     "ownerAccount": "user1",
     "ownerName": "张三",
     "readAuth": true,
     "writeAuth": true
  }
}
```

**响应字段说明**:

| 字段           | 类型    | 说明                     |
|--------------| ------- | ------------------------ |
| ownerAccount | String  | 文档所属账号             |
| ownerName    | String  | 文档所属名称             |
| readAuth     | Boolean | 当前用户是否有读权限     |
| writeAuth    | Boolean | 当前用户是否有写权限     |

**业务说明**:

- 系统会从Token中获取当前登录用户信息
- 获取当前文档的所属人信息
- 如果文档不存在，则自动创建文档记录，并保存 fileName（如果提供）
- 如果文档已存在但 fileName 为空，且请求中提供了 fileName，则更新 fileName
- fileName 仅在首次创建或为空时才会保存，后续不会覆盖
- 权限判断逻辑：
  - **文档所有者**：`readAuth = true`, `writeAuth = true`（同时拥有读写权限）
  - **被授权用户/部门**：`readAuth = true`, `writeAuth = false`（只有读权限）
  - **无权限用户**：`readAuth = false`, `writeAuth = false`（无任何权限）
- 前端根据 `readAuth` 和 `writeAuth` 的值决定展示哪些操作按钮

**错误码**:

| 状态码 | 说明                     |
| ------ | ------------------------ |
| 400    | 参数错误：文档id不能为空 |
| 401    | 未授权                   |

------
---

### 5. 获取文档密码

**接口地址**: `/doc/password`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌 |

**请求参数**:

| 参数名        | 类型    | 必填 | 说明                                            |
| ------------- | ------- | ---- | ----------------------------------------------- |
| docId         | String  | 是   | 文档唯一标识                                    |
| encryPassword | String  | 是   | 使用公钥加密后的密码                            |
| keyVersion    | String  | 否   | 密钥版本，默认"default"                         |
| isTemp        | Boolean | 否   | 是否为临时操作，true则跳过文件存在性和所有者校验，默认false |

**请求示例**:

```json
{
  "docId": "doc123456",
  "encryPassword": "encrypted_password_string",
  "keyVersion": "default",
  "isTemp": false
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
     "password": "document_password_123"
  }
}
```

**响应字段说明**:

| 字段     | 类型   | 说明                 |
| -------- | ------ | -------------------- |
| password | String | 解密后的文档密码明文 |

**业务说明**:

- 系统会从Token中获取当前登录用户信息
- 系统会校验当前登录用户是否有该文档的访问权限
- 只有被授权的用户才能获取文档密码
- 根据 `keyVersion` 参数查询对应版本的私钥进行解密
- 如果不传 `keyVersion`，默认使用 "default" 版本
- 前端应先调用 `/config/latest-key` 接口获取最新公钥和密钥版本，用于加密密码
- **临时模式**：当 `isTemp` 为 `true` 时，系统将跳过以下校验：
  - 不检查文件是否存在于数据库
  - 不检查当前用户是否为文件所有者
  - 不检查用户是否有访问权限
  - 直接进行密码解密操作
- **正常模式**：当 `isTemp` 为 `false` 或 `null` 时，执行完整的校验流程

**错误码**:

| 状态码 | 说明                                            |
| ------ | ----------------------------------------------- |
| 400    | 参数错误：docId和encryPassword不能为空          |
| 401    | 未授权或无访问权限                              |
| 409    | 文件不存在                                      |
| 500    | 解密失败                                        |

---

### 6. 获取文档权限树

**接口地址**: `/doc/auth/tree`

**请求方式**: `GET`

**请求参数**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| docId  | String | 否   | 文档唯一标识 |

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": [
    {
      "id": 1,
      "type": 0,
      "name": "武汉绿网",
      "account": null,
      "hasAuth": false,
      "deptList": [
        {
          "id": 2,
          "type": 0,
          "name": "总经理办公室",
          "account": null,
          "hasAuth": false,
          "deptList": [
            {
              "id": 3,
              "type": 0,
              "name": "行政部",
              "account": null,
              "hasAuth": false,
              "deptList": null,
              "employList": null
            },
            {
              "id": 4,
              "type": 0,
              "name": "政府事务与资质部",
              "account": null,
              "hasAuth": true,
              "deptList": null,
              "employList": [
                {
                  "id": 5,
                  "type": 1,
                  "name": "张贵丽",
                  "account": "zhanggl",
                  "deptList": null,
                  "employList": null,
                  "hasAuth": false
                },
                {
                  "id": 6,
                  "type": 1,
                  "name": "周丹",
                  "account": "zhoudan",
                  "deptList": null,
                  "employList": null,
                  "hasAuth": false
                }
              ]
            }
          ],
          "employList": [
            {
              "id": 7,
              "type": 1,
              "name": "孟春霞",
              "account": "mengcx",
              "deptList": null,
              "employList": null,
              "hasAuth": false
            }
          ]
        },
        {
          "id": 8,
          "type": 0,
          "name": "研发中心",
          "account": null,
          "hasAuth": false,
          "deptList": [
            {
              "id": 9,
              "type": 0,
              "name": "系统软件研发部",
              "account": null,
              "hasAuth": false,
              "deptList": [
                {
                  "id": 10,
                  "type": 0,
                  "name": "大数据技术组",
                  "account": null,
                  "hasAuth": false,
                  "deptList": null,
                  "employList": [
                    {
                      "id": 11,
                      "type": 1,
                      "name": "张鹏",
                      "account": "zhangpeng",
                      "deptList": null,
                      "employList": null,
                      "hasAuth": true
                    },
                    {
                      "id": 12,
                      "type": 1,
                      "name": "王志杰",
                      "account": "wangzj",
                      "deptList": null,
                      "employList": null,
                      "hasAuth": false
                    }
                  ]
                },
                {
                  "id": 13,
                  "type": 0,
                  "name": "云宽带系统研发组",
                  "account": null,
                  "hasAuth": false,
                  "deptList": null,
                  "employList": null
                }
              ],
              "employList": [
                {
                  "id": 14,
                  "type": 1,
                  "name": "孙昌燕",
                  "account": "suncy",
                  "deptList": null,
                  "employList": null,
                  "hasAuth": true
                }
              ]
            }
          ],
          "employList": [
            {
              "id": 15,
              "type": 1,
              "name": "牛晨光",
              "account": "niucg",
              "deptList": null,
              "employList": null,
              "hasAuth": false
            }
          ]
        }
      ]
    }
  ]
}
```

**响应字段说明**:

| 字段         | 类型               | 说明                                                        |
| ------------ | ------------------ | ----------------------------------------------------------- |
| id           | Long               | 节点 ID：type=0 为 sys_dept.id，type=1 为 sys_user.id      |
| type         | int                | 节点类型 0 部门 1员工                                       |
| name         | String             | 节点名称，部门为部门名称，员工为员工姓名                    |
| account      | String             | 账号名（用户专属，员工节点才有值）                          |
| hasAuth      | Boolean            | 是否有权限，默认false                                       |
| deptList     | Array[LdapNodeDTO] | 子部门列表（递归结构）                                      |
| employList   | Array[LdapNodeDTO] | 子员工列表（叶子节点，deptList 和 employList 均为 null）    |

**业务说明**:

- 返回完整的 LDAP 目录树结构
- 部门和员工使用不同的列表字段区分
- `deptList` 用于存储子部门，可继续嵌套
- `employList` 用于存储该节点下的员工，为叶子节点
- `hasAuth` 用于判断节点是否有权限


---

### 7. 更新文档权限

**接口地址**: `/doc/auth/update`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌 |

**请求参数**:

| 参数名      | 类型       | 必填 | 说明                                            |
| ----------- | ---------- | ---- | ----------------------------------------------- |
| docId       | String     | 是   | 文档唯一标识                                    |
| userIdList  | Array[Long]| 否   | 授权的用户 ID 列表（sys_user.id）               |
| deptIdList  | Array[Long]| 否   | 授权的部门 ID 列表（sys_dept.id）               |
| isTemp      | Boolean    | 否   | 是否为临时操作，true则跳过文件存在性和所有者校验，默认false |

**请求示例**:

```json
{
  "docId": "doc123456",
  "userIdList": [10, 20],
  "deptIdList": [3]
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": "操作成功"
}
```

**业务说明**:

- 系统会从Token中获取当前登录用户信息
- **仅文档所有者**可以更新授权关系
- 更新时会先删除该文档的所有旧授权记录，然后插入新的授权记录
- userIdList（sys_user.id）和 deptIdList（sys_dept.id）可以同时为空，表示取消所有授权
- 传入的是内部主键 ID 列表，而非 LDAP DN
- 系统始终执行完整校验流程：校验文件是否存在、当前用户是否为文档所有者（仅所有者可更新授权）

**错误码**:

| 状态码 | 说明                     |
| ------ | ------------------------ |
| 400    | 参数错误：docId不能为空  |
| 401    | 未授权                   |
| 409    | 文件不存在或无操作权限   |
| 500    | 系统异常                 |
---

### 8. 上报保存记录

**接口地址**: `/doc/save/log`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌 |

**请求参数**:

| 参数名          | 类型          | 必填 | 说明                   |
| --------------- | ------------- | ---- | ---------------------- |
| docId           | String        | 是   | 文档唯一标识           |
| path            | String        | 是   | 文件完整路径           |
| keyVersion      | String        | 是   | 密钥版本号             |
| beforePassword  | String        | 否   | 之前的密码（ECC加密）  |
| afterPassword   | String        | 否   | 当前的密码（ECC加密）  |
| possiblePassword | Array[String] | 否   | 可能的密码集合（ECC加密）|
| platform        | String        | 否   | 操作来源平台（win/android） |

**请求示例**:

```json
{
  "docId": "doc123456",
  "path": "c://desktop//a.docx",
  "keyVersion": "default",
  "beforePassword": "encrypted_before_password",
  "afterPassword": "encrypted_after_password",
  "possiblePassword": ["encrypted_pwd1", "encrypted_pwd2", "encrypted_pwd3"],
  "platform": "win"
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": "操作成功"
}
```

**业务说明**:

- 系统会从Token中获取当前登录用户信息
- 根据 `keyVersion` 参数查询对应的私钥配置
- 使用 ECC 私钥对 `beforePassword`、`afterPassword`、`possiblePassword` 进行解密
- 记录用户点击保存文档时，插件此时的密码元数据信息（解密后的明文）
- 每次插件写密码都会上报这些元数据信息
- 方便后续找回密码和溯源
- `platform` 字段用于标识操作来源平台，可选值为 "win" 或 "android"
- **重要**：所有密码字段必须使用 ECC 公钥加密后传输，确保安全性

**错误码**:

| 状态码 | 说明                                      |
| ------ | ----------------------------------------- |
| 400    | 参数错误：docId、path和keyVersion不能为空 |
| 500    | beforePassword解密失败                    |
| 500    | afterPassword解密失败                     |
| 500    | possiblePasswordList第N个密码解密失败     |
| 500    | 未找到对应的配置密钥                      |
| 500    | 系统异常                                  |

---

### 9. 获取LDAP配置

**接口地址**: `/config/ldap`

**请求方式**: `GET`

**请求参数**: 无

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
    "url": "ldap://192.168.1.100:389",
    "base": "DC=greenet,DC=com,DC=cn",
    "username": "admin@greenet.com.cn",
    "trees": [
      "OU=武汉绿网,OU=绿色网络,DC=greenet,DC=com,DC=cn"
    ],
    "syncTimes": "10:00,20:00",
    "syncEnabled": true
  }
}
```

**响应字段说明**:

| 字段        | 类型          | 说明                                  |
| ----------- | ------------- | ------------------------------------- |
| url         | String        | LDAP服务器地址                        |
| base        | String        | 目录根 DN（登录搜索基与目录边界）      |
| username    | String        | LDAP管理员账号                        |
| trees       | Array[String] | LDAP组织树根节点列表                  |
| syncTimes   | String        | 定时全量同步时刻（逗号分隔）          |
| syncEnabled | Boolean       | 定时同步总开关                        |

**业务说明**:

- 获取当前系统的LDAP配置信息
- 出于安全考虑，不返回LDAP密码
- 无需认证即可访问

**错误码**:

| 状态码 | 说明     |
| ------ | -------- |
| 500    | 系统异常 |

---

### 10. 更新LDAP配置

**接口地址**: `/config/ldap`

**请求方式**: `PUT`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌（需 admin 角色） |

**请求参数**:

| 参数名      | 类型          | 必填 | 说明                                          |
| ----------- | ------------- | ---- | --------------------------------------------- |
| url         | String        | 否   | LDAP服务器地址                                |
| baseDn      | String        | 否   | 目录根 DN                                     |
| username    | String        | 否   | LDAP绑定账号                                  |
| password    | String        | 否   | LDAP绑定密码（留空表示不修改现有密码）         |
| trees       | Array[String] | 否   | LDAP组织树根节点列表（每条独立作为同步根）     |
| syncTimes   | String        | 否   | 定时全量同步时刻（逗号分隔，如 "10:00,20:00"）|
| syncEnabled | Boolean       | 否   | 定时同步总开关                                |

**请求示例**:

```json
{
  "url": "ldap://192.168.1.100:389",
  "baseDn": "DC=greenet,DC=com,DC=cn",
  "username": "admin@greenet.com.cn",
  "password": "",
  "trees": ["OU=武汉绿网,OU=绿色网络,DC=greenet,DC=com,DC=cn"],
  "syncTimes": "10:00,20:00",
  "syncEnabled": true
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": "配置更新成功"
}
```

**业务说明**:

- 需 admin 角色调用（携带 token 请求头）。
- 更新LDAP配置并自动重新加载。
- 可以只传入需要更新的字段（增量更新）。
- `password` 为空字符串表示保留原密码；非空则覆盖。
- 更新后立即生效，无需重启服务。

**错误码**:

| 状态码 | 说明         |
| ------ | ------------ |
| 401    | 未登录或登录过期 |
| 403    | 无管理员权限 |
| 500    | 配置更新失败 |

---

### 11. 刷新配置

**接口地址**: `/config/refresh`

**请求方式**: `GET`

**请求参数**: 无

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": "配置刷新成功"
}
```

**业务说明**:

- 从数据库重新加载所有配置
- 当手动修改数据库配置后，可调用此接口使配置生效
- 无需重启服务

**错误码**:

| 状态码 | 说明         |
| ------ | ------------ |
| 500    | 配置刷新失败 |

---

### 12. 公钥加密

**接口地址**: `/config/encrypt`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌（需 admin 角色） |

**Content-Type**: `application/json`

**请求参数**:

| 参数名     | 类型   | 必填 | 说明                                       |
| ---------- | ------ | ---- | ------------------------------------------ |
| text       | String | 是   | 待加密的原始字符串                         |
| keyVersion | String | 否   | 密钥版本，默认"default"，可使用"latest"获取最新密钥 |

**请求示例**:

```json
{
  "text": "test123",
  "keyVersion": "default"
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
    "encryptedText": "base64_encoded_encrypted_string",
    "originalLength": "7",
    "encryptedLength": "128",
    "keyVersion": "default"
  }
}
```

**响应字段说明**:

| 字段            | 类型   | 说明                 |
| --------------- | ------ | -------------------- |
| encryptedText   | String | ECC加密后的密文      |
| originalLength  | String | 原始明文长度         |
| encryptedLength | String | 加密后密文长度       |
| keyVersion      | String | 使用的密钥版本       |

**业务说明**:

- 使用系统公钥对传入的字符串进行ECC加密（ECIES方案）
- 主要用于前端加密密码后传给后端
- 可通过 `keyVersion` 参数指定使用哪个版本的公钥
- 如果传入 "latest"，将使用优先级最高的公钥（order_num最大）
- 如果不传 `keyVersion`，默认使用 "default" 版本

**使用示例**:

```bash
# 使用默认密钥版本
curl -X POST "http://localhost:8080/config/encrypt" \
  -H "Content-Type: application/json" \
  -d '{"text":"test123","keyVersion":"default"}'

# 使用指定密钥版本
curl -X POST "http://localhost:8080/config/encrypt" \
  -H "Content-Type: application/json" \
  -d '{"text":"test123","keyVersion":"v2"}'

# 使用最新密钥版本（推荐）
curl -X POST "http://localhost:8080/config/encrypt" \
  -H "Content-Type: application/json" \
  -d '{"text":"test123","keyVersion":"latest"}'
```

**错误码**:

| 状态码 | 说明                     |
| ------ | ------------------------ |
| 400    | 参数错误：text不能为空   |
| 500    | 加密失败：系统异常       |

---

---

### 13. 私钥解密

**接口地址**: `/config/decrypt`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌（需 admin 角色） |

**Content-Type**: `application/json`

**请求参数**:

| 参数名         | 类型   | 必填 | 说明                                       |
| -------------- | ------ | ---- | ------------------------------------------ |
| encryptedText  | String | 是   | ECC加密后的密文                            |
| keyVersion     | String | 否   | 密钥版本，默认"default"，可使用"latest"获取最新密钥 |

**请求示例**:

```json
{
  "encryptedText": "base64_encoded_encrypted_string",
  "keyVersion": "default"
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
    "decryptedText": "password123",
    "keyVersion": "default"
  }
}
```

**响应字段说明**:

| 字段          | 类型   | 说明                 |
| ------------- | ------ | -------------------- |
| decryptedText | String | 解密后的明文         |
| keyVersion    | String | 使用的密钥版本       |

**业务说明**:

- 使用系统私钥对传入的密文进行ECC解密（ECIES方案）
- 主要用于服务端解密客户端上传的加密数据
- 可通过 `keyVersion` 参数指定使用哪个版本的私钥
- 如果传入 "latest"，将使用优先级最高的私钥（order_num最大）
- 如果不传 `keyVersion`，默认使用 "default" 版本
- **注意**：此接口涉及私钥操作，已要求 admin 角色鉴权（携带 token 请求头）。

**使用示例**:

```bash
# 使用默认密钥版本解密
curl -X POST "http://localhost:8080/config/decrypt" \
  -H "Content-Type: application/json" \
  -d '{"encryptedText":"base64_encoded_string","keyVersion":"default"}'

# 使用最新密钥版本解密
curl -X POST "http://localhost:8080/config/decrypt" \
  -H "Content-Type: application/json" \
  -d '{"encryptedText":"base64_encoded_string","keyVersion":"latest"}'
```

**错误码**:

| 状态码 | 说明                             |
| ------ | -------------------------------- |
| 400    | 参数错误：encryptedText不能为空  |
| 500    | 解密失败：系统异常               |
| 500    | 系统配置错误：未找到密钥版本     |

---

### 14. 获取最新密钥信息

**接口地址**: `/config/latest-key`

**请求方式**: `GET`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌（需 admin 角色） |

**请求参数**: 无

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": {
    "keyVersion": "default",
    "publicKey": "MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEuY2/Hz7c7gM0O8P/8VYjDasWhdW4jyS99+Xwyghe+CVFko7KPeamzaOsUffIHQz0VAA8RH9MV1BYyuZAJ7X05Q=="
  }
}
```

**响应字段说明**:

| 字段       | 类型   | 说明                       |
| ---------- | ------ | -------------------------- |
| keyVersion | String | 当前优先级最高的密钥版本   |
| publicKey  | String | ECC公钥（Base64编码）      |

**业务说明**:

- 获取系统当前优先级最高的密钥版本和公钥
- 从 `config_secret_key` 表中查询 `order_num` 最大的记录
- `order_num` 越大，优先级越高
- 前端应在初始化时调用此接口获取公钥，用于加密密码
- 建议缓存该信息，避免频繁请求
- 当需要密钥轮换时，插入新密钥并设置更大的 `order_num`，客户端再次调用此接口即可获取新密钥

**使用场景**:

1. **客户端初始化**: 启动时获取公钥用于加密数据
2. **密钥轮换**: 定期调用此接口检查是否有新密钥
3. **多版本管理**: 根据 `keyVersion` 选择合适的密钥进行加解密

**错误码**:

| 状态码 | 说明                         |
| ------ | ---------------------------- |
| 500    | 系统配置错误：未找到配置密钥 |

---

### 15. 修改密码

**接口地址**: `/account/change-password`

**请求方式**: `POST`

**请求头**:

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| token  | String | 是   | 登录返回的令牌 |

**请求参数**:

| 参数名      | 类型   | 必填 | 说明   |
| ----------- | ------ | ---- | ------ |
| oldPassword | String | 是   | 原密码 |
| newPassword | String | 是   | 新密码 |

**请求示例**:

```json
{
  "oldPassword": "oldpass123",
  "newPassword": "newpass456"
}
```

**响应示例**:

```json
{
  "message": "操作成功",
  "status": 200,
  "data": "密码修改成功"
}
```

**业务说明**:

- 仅本地用户（LOCAL）可修改密码；LDAP 用户不支持，返回 400。
- 系统从 Token 中获取当前登录用户，校验原密码通过后更新为新密码。

**错误码**:

| 状态码 | 说明                                       |
| ------ | ------------------------------------------ |
| 400    | 参数错误 / 原密码错误 / LDAP 用户不支持修改 |
| 401    | 未登录或登录过期                           |
| 500    | 系统异常                                   |

---